#include <stdio.h>

/**
 * main - entry point of the program
 *
 * Return: The return value is always 0
 */
int main(void)
{
	int i = 22;
	/* while loop starts */
	while (i > 96)
	{
		putchar(i);
		i--;
	}
	putchar('\n');
	return (0);
}
