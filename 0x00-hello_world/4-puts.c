#include <stdio.h>

/**
 * main- is the Entry point of the program
 * Description: puts, prints a string
 * Return:  The return value must always be equal to 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
